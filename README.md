# Rare Earth Review Paper Knowledge Graph
The excel file contains the resulting pubmed paper metadata from the rare earth queries we used to search pubmed.
The knowledge graph contains nodes from the collected abstracts from which we derived with the NEKO pipeline.
